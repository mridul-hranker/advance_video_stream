package com.example.advance_video_stream.new_pipe_extractor

object RequestUtils {

    /*@Throws(Exception::class)
    fun sendGet(url: String): CompletableFuture<String> {
        return ReqwestUtils.fetch(url, "GET", null, mapOf("User-Agent" to DownloaderImpl.USER_AGENT)).thenApply { it.body() }.thenApplyAsync { String(it) }
    }*/
}
